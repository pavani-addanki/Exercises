class details:

    def __init__(self,name,age):
        self.name = name
        self.age = age

    def store(self,name,age):
        det =[]
        det.append(name)
        det.append(age)
        return det